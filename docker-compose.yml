version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_db
    ports:
      - "6333:6333" # REST API port
      - "6334:6334" # gRPC port (faster, used by the Python client)
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: always